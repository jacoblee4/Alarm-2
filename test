var gr = new GlideRecord('u_imp_alarm_codes');
gr.setLimit(5);
gr.query();
while (gr.next()) {
    gs.info('ROW >> state=[' + gr.getValue('sys_import_state') +
            '] comment=[' + gr.getValue('sys_import_state_comment') +
            '] target=[' + gr.getValue('sys_target_sys_id') + ']');
}
