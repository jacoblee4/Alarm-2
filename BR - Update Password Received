Before insert or update

condiiton: current.alarm_password.changes()

(function executeRule(current, previous /*null when async*/) {

// Check if alarm_password is empty and set password_received accordingly
if (gs.nil(current.alarm_password)) {
	current.password_received = false;
} else {
	current.password_received = true;
}	

})(current, previous);
