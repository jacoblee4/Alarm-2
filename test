var gr = new GlideRecord('sys_import_log');
gr.orderByDesc('sys_created_on');
gr.setLimit(20);
gr.query();
while (gr.next()) {
    gs.info('LOG >> [' + gr.getValue('level') + '] ' + gr.getValue('message'));
}
