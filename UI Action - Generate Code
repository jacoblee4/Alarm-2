Client checked

condition: current.issue_type == 'alarm_panel' && current.request_type == 'add' && (current.state == 1 || current.state== 41)

workspace client script:
function onClick(g_form) {
    var ga = new GlideAjax('x_banun_bunow_al_0.AlarmCodeAjax');
    ga.addParam('sysparm_name', 'generateUniqueCode');

    ga.getXMLAnswer(function(response) {
        if (response == "ERROR") {
            g_form.addErrorMessage('Cloud not generate a unique code. Please try again or contact an admin.');
            return;
        }

        g_form.setValue('alarm_code', response);
    });
}
