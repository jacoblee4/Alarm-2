client checked

Condition: current.issue_type == 'alarm_panel' && current.request_type == 'add' &&  current.alarm_password == '' && current.requested_password == false


Workspace Client Script:
function onClick(g_form) {
    g_form.submit('request_password');
}


UI Action Script:
var openedFor = current.getValue('x_banun_bunow_al_0_opened_for');

gs.eventQueue(
    'x_banun_property_0.request.password',
    current,
    openedFor,
    ''
);

gs.addInfoMessage('Password request email was triggered.');

current.work_notes = 'Email request sent to set a password.';
current.setValue('requested_password', true);
current.setValue('password_email_sent', new GlideDateTime().getValue());
current.update();
