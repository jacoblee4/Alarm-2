After Insert

Filter Conditions
issue_type=alarm_panel^request_type=add^alarm_passwordISEMPTY^EQ

(function executeRule(current, previous /*null when async*/ ) {
    new x_banun_bunow_al_0.AlarmCodeHelper().applyCredentials(current.getUniqueValue());
})(current, previous);
