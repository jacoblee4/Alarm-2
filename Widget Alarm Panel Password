//HTML

<div class="aw-password-widget">

  <div class="aw-state"
    ng-class="{
      'aw-loading':   c.state === 'loading',
      'aw-denied':    c.state === 'no_record' || c.state === 'wrong_type' || c.state === 'denied' || c.state === 'error',
      'aw-submitted': c.state === 'submitted',
      'aw-form':      c.state === 'form',
      'aw-success':   c.state === 'success'
    }">

    <!-- ===== LOADING ===== -->
    <div ng-if="c.state === 'loading'" class="aw-state-content">
      <i class="fa fa-spinner fa-spin fa-2x"></i>
      <p>Loading...</p>
    </div>

    <!-- ===== NO RECORD ===== -->
    <div ng-if="c.state === 'no_record'" class="aw-state-content">
      <div class="aw-icon-wrap denied-icon">
        <i class="fa fa-times-circle"></i>
      </div>
      <h2>No Record Found</h2>
      <p>{{c.errorMessage || 'No reference record found. Please contact an administrator.'}}</p>
    </div>

    <!-- ===== WRONG TYPE ===== -->
    <div ng-if="c.state === 'wrong_type'" class="aw-state-content">
      <div class="aw-icon-wrap denied-icon">
        <i class="fa fa-times-circle"></i>
      </div>
      <h2>No Password Required</h2>
      <p>This record does not need a password submitted. Password setup is only required for alarm additions.</p>
    </div>

    <!-- ===== ACCESS DENIED ===== -->
    <div ng-if="c.state === 'denied'" class="aw-state-content">
      <div class="aw-icon-wrap denied-icon">
        <i class="fa fa-times-circle"></i>
      </div>
      <h2>Access Restricted</h2>
      <p>Only the <strong>Opened For</strong> user assigned to this work order can view and submit this content.</p>
    </div>

    <!-- ===== ALREADY SUBMITTED ===== -->
    <div ng-if="c.state === 'submitted'" class="aw-state-content">
      <div class="aw-icon-wrap submitted-icon">
        <i class="fa fa-check-circle"></i>
      </div>
      <h2>Password Already Submitted</h2>
      <p>A password has already been submitted to the alarm panel team. Please wait for a follow-up email indicating your alarm code and password have been created.</p>
    </div>

    <!-- ===== PASSWORD FORM ===== -->
    <div ng-if="c.state === 'form'" class="aw-state-content">
      <div class="aw-header">
        <div class="aw-icon-wrap form-icon">
          <i class="fa fa-unlock"></i>
        </div>
        <h2>Set Your Alarm Password</h2>
        <p class="aw-subtitle">Please create a password for your alarm code below.</p>
      </div>

      <!-- Requirements Checklist -->
      <div class="aw-requirements">
        <p class="aw-req-title">Password Requirements</p>
        <ul class="aw-req-list">
          <li class="aw-req-item" ng-class="!c.password ? 'rule-neutral' : (c.rules.minLength ? 'rule-pass' : 'rule-fail')">
            <span class="aw-req-icon">
              <i ng-if="!c.password" class="fa fa-circle-o"></i>
              <i ng-if="c.password && c.rules.minLength" class="fa fa-check"></i>
              <i ng-if="c.password && !c.rules.minLength" class="fa fa-times"></i>
            </span>
            Minimum 4 letters
          </li>
          <li class="aw-req-item" ng-class="!c.password ? 'rule-neutral' : (c.rules.maxLength ? 'rule-pass' : 'rule-fail')">
            <span class="aw-req-icon">
              <i ng-if="!c.password" class="fa fa-circle-o"></i>
              <i ng-if="c.password && c.rules.maxLength" class="fa fa-check"></i>
              <i ng-if="c.password && !c.rules.maxLength" class="fa fa-times"></i>
            </span>
            Maximum 10 letters
          </li>
          <li class="aw-req-item" ng-class="!c.password ? 'rule-neutral' : (c.rules.lettersOnly ? 'rule-pass' : 'rule-fail')">
            <span class="aw-req-icon">
              <i ng-if="!c.password" class="fa fa-circle-o"></i>
              <i ng-if="c.password && c.rules.lettersOnly" class="fa fa-check"></i>
              <i ng-if="c.password && !c.rules.lettersOnly" class="fa fa-times"></i>
            </span>
            Letters only — no numbers or special characters
          </li>
        </ul>
      </div>

      <!-- Password Field -->
      <div class="aw-field-wrap">
        <label class="aw-label" for="alarmPassword">Password</label>
        <div class="aw-input-wrap" ng-class="{'has-error': c.touched && !c.valid, 'has-success': c.touched && c.valid}">
          <input
            id="alarmPassword"
            type="{{c.showPassword ? 'text' : 'password'}}"
            class="aw-input"
            ng-model="c.password"
            ng-change="c.onPasswordChange()"
            ng-blur="c.onBlur()"
            placeholder="Enter your password"
            autocomplete="new-password"
            maxlength="10"
          />
          <button class="aw-toggle-vis" ng-click="c.showPassword = !c.showPassword" type="button" aria-label="Toggle password visibility">
            <i ng-class="c.showPassword ? 'fa fa-eye-slash' : 'fa fa-eye'"></i>
          </button>
        </div>
      </div>

      <!-- Error Banner -->
      <div class="aw-error-banner" ng-if="c.serverError">
        <i class="fa fa-exclamation-triangle"></i>
        {{c.serverError}}
      </div>

      <!-- Submit Button -->
      <button
        class="aw-submit-btn"
        ng-click="c.submit()"
        ng-disabled="!c.valid || c.submitting"
        type="button"
      >
        <span ng-if="!c.submitting">Submit Password</span>
        <span ng-if="c.submitting"><i class="fa fa-spinner fa-spin"></i> Submitting...</span>
      </button>
    </div>

    <!-- ===== SUCCESS ===== -->
    <div ng-if="c.state === 'success'" class="aw-state-content">
      <div class="aw-icon-wrap success-icon">
        <i class="fa fa-check-circle"></i>
      </div>
      <h2>Thank You!</h2>
      <p>Your response has been sent to the alarm panel team. Please wait for a follow-up email indicating your alarm code and password have been created.</p>
    </div>

    <!-- ===== ERROR ===== -->
    <div ng-if="c.state === 'error'" class="aw-state-content">
      <div class="aw-icon-wrap denied-icon">
        <i class="fa fa-times-circle"></i>
      </div>
      <h2>Something Went Wrong</h2>
      <p>Your password could not be saved. Please contact an administrator for assistance.</p>
    </div>

  </div>

</div>















//CSS

.aw-password-widget {
  font-family: "Source Sans Pro", "Helvetica Neue", Arial, sans-serif;
  max-width: 480px;
  margin: 40px auto;
  padding: 0 16px;
}

/* ----- Shared State Container ----- */
.aw-state {
  background: #ffffff;
  border-radius: 12px;
  box-shadow: 0 4px 24px rgba(0, 0, 0, 0.08), 0 1px 4px rgba(0, 0, 0, 0.06);
  padding: 40px 36px;
  text-align: center;
}

.aw-state-content {
  animation: aw-fade-in 0.25s ease;
}

@keyframes aw-fade-in {
  from { opacity: 0; }
  to   { opacity: 1; }
}

.aw-state h2 {
  font-size: 22px;
  font-weight: 700;
  color: #1a1f36;
  margin: 16px 0 8px;
}

.aw-state p {
  font-size: 15px;
  color: #525f7f;
  line-height: 1.6;
  margin: 0;
}

/* ----- Icon Circles ----- */
.aw-icon-wrap {
  width: 68px;
  height: 68px;
  border-radius: 50%;
  display: flex;
  align-items: center;
  justify-content: center;
  margin: 0 auto 8px;
  font-size: 32px;
}

.denied-icon    { background: #fff1f0; color: #f5222d; }
.submitted-icon { background: #fff7e6; color: #fa8c16; }
.form-icon      { background: #e6f7ff; color: #1890ff; }
.success-icon   { background: #f6ffed; color: #52c41a; }

/* ----- Loading ----- */
.aw-loading p {
  color: #8c8c8c;
  margin-top: 12px;
}

/* ----- Form Layout ----- */
.aw-form {
  text-align: left;
}

.aw-header {
  text-align: center;
  margin-bottom: 28px;
}

.aw-subtitle {
  font-size: 14px;
  color: #8c96b0;
}

/* ----- Field ----- */
.aw-field-wrap {
  margin-bottom: 20px;
}

.aw-label {
  display: block;
  font-size: 13px;
  font-weight: 600;
  color: #3d4559;
  margin-bottom: 6px;
  letter-spacing: 0.02em;
  text-transform: uppercase;
}

.aw-input-wrap {
  position: relative;
  border-radius: 8px;
  border: 1.5px solid #d9dde8;
  background: #fafbfc;
  transition: border-color 0.2s, box-shadow 0.2s;
}

.aw-input-wrap:focus-within {
  border-color: #1890ff;
  box-shadow: 0 0 0 3px rgba(24, 144, 255, 0.12);
  background: #fff;
}

.aw-input-wrap.has-error {
  border-color: #f5222d;
  box-shadow: 0 0 0 3px rgba(245, 34, 45, 0.1);
}

.aw-input-wrap.has-success {
  border-color: #52c41a;
  box-shadow: 0 0 0 3px rgba(82, 196, 26, 0.1);
}

.aw-input {
  width: 100%;
  padding: 11px 44px 11px 14px;
  border: none;
  background: transparent;
  font-size: 16px;
  color: #1a1f36;
  outline: none;
  box-sizing: border-box;
  border-radius: 8px;
  letter-spacing: 0.05em;
}

.aw-toggle-vis {
  position: absolute;
  right: 10px;
  top: 50%;
  transform: translateY(-50%);
  background: none;
  border: none;
  cursor: pointer;
  color: #8c96b0;
  padding: 4px;
  line-height: 1;
  font-size: 18px;
  transition: color 0.2s;
}

.aw-toggle-vis:hover { color: #1890ff; }

/* ----- Requirements List ----- */
.aw-requirements {
  background: #f5f7fa;
  border-radius: 8px;
  padding: 16px 18px;
  margin-bottom: 24px;
}

.aw-req-title {
  font-size: 12px;
  font-weight: 700;
  text-transform: uppercase;
  letter-spacing: 0.06em;
  color: #8c96b0;
  margin: 0 0 10px;
}

.aw-req-list {
  list-style: none;
  padding: 0;
  margin: 0;
}

.aw-req-item {
  display: flex;
  align-items: center;
  gap: 10px;
  font-size: 14px;
  color: #525f7f;
  padding: 5px 0;
  transition: color 0.2s;
}

.aw-req-icon {
  font-size: 17px;
  line-height: 1;
  flex-shrink: 0;
  width: 20px;
  text-align: center;
}

.rule-neutral .aw-req-icon { color: #c0c8d8; }
.rule-pass    { color: #2f9e44; }
.rule-pass    .aw-req-icon { color: #52c41a; }
.rule-fail    { color: #c92a2a; }
.rule-fail    .aw-req-icon { color: #f5222d; }

/* ----- Error Banner ----- */
.aw-error-banner {
  display: flex;
  align-items: center;
  gap: 8px;
  background: #fff1f0;
  border: 1px solid #ffa39e;
  border-radius: 8px;
  padding: 10px 14px;
  color: #c0392b;
  font-size: 14px;
  margin-bottom: 16px;
}

/* ----- Submit Button ----- */
.aw-submit-btn {
  width: 100%;
  padding: 13px;
  background: #1890ff;
  color: #fff;
  border: none;
  border-radius: 8px;
  font-size: 15px;
  font-weight: 600;
  cursor: pointer;
  display: flex;
  align-items: center;
  justify-content: center;
  gap: 8px;
  transition: background 0.2s, opacity 0.2s, transform 0.1s;
}

.aw-submit-btn:hover:not([disabled]) {
  background: #096dd9;
  transform: translateY(-1px);
}

.aw-submit-btn:active:not([disabled]) {
  transform: translateY(0);
}

.aw-submit-btn[disabled] {
  background: #bdc5d4;
  cursor: not-allowed;
}













//Server Script

(function() {
    data.state = '';
    data.success = false;
    data.error = '';

    // ── Guard: input object safety check ──────────────
    if (!input || typeof input !== 'object') {
        data.state = 'no_record';
        data.errorMessage = 'No reference record found. Please contact an administrator.';
        return;
    }

    var action = input.action;
    var sysId = input.sys_id;

    // ── Guard: must have a sys_id ──────────────────────
    if (!sysId) {
        data.state = 'no_record';
        data.errorMessage = 'No reference record found. Please contact an administrator.';
        return;
    }

    // ── Load the work order record ─────────────────────
    var wo = new GlideRecord('x_banun_property_0_physical_security_work_order');
    if (!wo.get(sysId)) {
        data.state = 'denied';
        return;
    }

    // ── Check: request type must be 'add' ─────────────
    var requestType = wo.getValue('request_type');
    if (!requestType || requestType.toLowerCase() !== 'add') {
        data.state = 'wrong_type';
        return;
    }

    // ── Check: already submitted? ──────────────────────
    var alarmPassword = wo.getDisplayValue('alarm_password');
    if (alarmPassword && alarmPassword.trim() !== '') {
        data.state = 'submitted';
        return;
    }

    // ── Check: logged-in user matches opened_for ───────
    var openedForSysId = wo.getValue('x_banun_bunow_al_0_opened_for');
    var currentUser = gs.getUserID();

    if (openedForSysId !== currentUser) {
        data.state = 'denied';
        return;
    }

    // ── INIT action ────────────────────────────────────
    if (action === 'init') {
        data.state = 'form';
        return;
    }

    // ── SUBMIT action ──────────────────────────────────
    if (action === 'submit') {
        var password = input.password || '';

        var result = new x_banun_bunow_al_0.AlarmCodeHelper().submitPassword(sysId, password);

        data.success = result.success;
        data.error = result.error;
        if (result.state) {
            data.state = result.state;
        }
        return;
    }

    // Fallback
    data.state = 'denied';

})();





















//Client Script

api.controller = function($location) {
  var c = this;

  // ── State ──────────────────────────────────────────
  c.state        = 'loading';
  c.password     = '';
  c.showPassword = false;
  c.touched      = false;
  c.valid        = false;
  c.submitting   = false;
  c.serverError  = '';
  c.errorMessage = '';

  c.rules = {
    minLength:   false,
    maxLength:   false,
    lettersOnly: false
  };

  // ── Init ───────────────────────────────────────────
  c.$onInit = function() {
    var sysId = $location.search().sys_id || c.options.sys_id || '';

    if (!sysId) {
      c.state        = 'no_record';
      c.errorMessage = 'No reference record found. Please contact an administrator.';
      return;
    }

    c.server.get({ action: 'init', sys_id: sysId }).then(function(r) {
      var d = r.data;
      if (d && d.state) {
        c.state        = d.state;
        c.errorMessage = d.errorMessage || '';
      } else {
        c.state        = 'no_record';
        c.errorMessage = 'Unexpected response. Please contact an administrator.';
      }
    }).catch(function() {
      c.state        = 'no_record';
      c.errorMessage = 'An unexpected error occurred. Please contact an administrator.';
    });
  };

  // ── Password Validation ────────────────────────────
  c.onPasswordChange = function() {
    var p = c.password || '';
    var lettersOnly = /^[A-Za-z]+$/.test(p) && p.length > 0;
    c.rules.minLength   = p.length >= 4 && lettersOnly;
    c.rules.maxLength   = p.length > 0 && p.length <= 10 && lettersOnly;
    c.rules.lettersOnly = lettersOnly;
    c.valid = c.rules.minLength && c.rules.maxLength && c.rules.lettersOnly;
  };

  c.onBlur = function() {
    c.touched = true;
    c.onPasswordChange();
  };

  // ── Submit ─────────────────────────────────────────
  c.submit = function() {
    c.touched = true;
    c.onPasswordChange();

    if (!c.valid || c.submitting) return;

    var sysId = $location.search().sys_id || c.options.sys_id || '';
    c.submitting  = true;
    c.serverError = '';

    c.server.get({
      action:   'submit',
      sys_id:   sysId,
      password: c.password
    }).then(function(r) {
      c.submitting = false;
      if (r.data.success) {
        c.state = 'success';
      } else if (r.data.state === 'error') {
        c.state = 'error';
      } else {
        c.serverError = r.data.error || 'An unexpected error occurred. Please try again.';
      }
    }).catch(function() {
      c.submitting = false;
      c.state      = 'error';
    });
  };

};
