try {
    gs.info('A: ' + new x_banun_bunow_al_0.AlarmCodeHelper().generateUniqueCode());
} catch (e) {
    gs.error('A EX: ' + e);
}

try {
    gs.info('B: ' + new AlarmCodeAjax().generateUniqueCode());
} catch (e) {
    gs.error('B EX: ' + e);
}
